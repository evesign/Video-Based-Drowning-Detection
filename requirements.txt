# =====================================================================
# Video-Based Drowning Detection — requirements
# Python 3.10 / CUDA GPU 환경 기준 (이 저장소 코드는 GPU를 전제로 함)
# =====================================================================

# --- PyTorch (CUDA 버전에 맞게 설치 권장) -----------------------------
# 아래 버전은 검증된 조합이지만, 본인 GPU의 CUDA에 맞는 빌드를
# https://pytorch.org/get-started/locally/ 에서 설치하는 것을 권장.
torch==2.6.0
torchvision==0.21.0

# --- 객체 탐지 / 추적 (YOLO + SAM2) ----------------------------------
ultralytics==8.4.21          # YOLO 사람 탐지
hydra-core==1.3.2            # SAM2 config 로딩
opencv-python==4.13.0.92     # cv2 — 영상 입출력 / 시각화
numpy==2.2.6
# 주의: SAM2(streaming, camera predictor)는 pip 패키지가 아니라
#       sam2_streaming/ 폴더로 직접 클론해야 함 (README의 Setup 참고)

# --- 행동 인식 (VideoMAE v2 + LoRA) ----------------------------------
transformers==4.57.0
peft==0.19.1                 # LoRA
decord==0.6.0                # 빠른 비디오 디코딩
scikit-learn==1.8.0          # P/R/F1, confusion matrix
pandas==2.3.3                # 평가 결과 표
matplotlib==3.10.5           # loss curve 등

# =====================================================================
# [선택] VLM 추론 모듈 (vlm/) — 별도 conda 환경 권장
# vllm 은 torch 버전을 강하게 고정하므로, 위 환경과 분리해서 설치할 것.
#   예) conda create -n qwen35 python=3.10 && pip install vllm openai
# ---------------------------------------------------------------------
# vllm
# openai
